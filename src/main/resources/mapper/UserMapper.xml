<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.community.entity.User">
        <id property="id" column="id"/>
        <result property="openid" column="openid"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="phone" column="phone"/>
        <result property="realName" column="real_name"/>
        <result property="avatar" column="avatar"/>
        <result property="gender" column="gender"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="points" column="points"/>
        <result property="volunteerHours" column="volunteer_hours"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM t_user WHERE username = #{username} AND deleted = 0
    </select>
    <select id="selectByOpenid" resultMap="BaseResultMap">
        SELECT * FROM t_user WHERE openid = #{openid} AND deleted = 0
    </select>
    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT * FROM t_user WHERE phone = #{phone} AND deleted = 0
    </select>
    <update id="updatePoints">
        UPDATE t_user SET points = points + #{points} WHERE id = #{userId}
    </update>
</mapper>
