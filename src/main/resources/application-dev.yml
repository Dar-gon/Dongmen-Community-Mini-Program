# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/community_dev?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 123456

  redis:
    host: localhost
    port: 6379
    password:

  # 开发环境禁用Nacos
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false

logging:
  level:
    root: INFO
    com.community: DEBUG

file:
  upload:
    path: /data/uploads/dev/

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: community-dev
