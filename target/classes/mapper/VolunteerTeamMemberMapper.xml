<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.mapper.VolunteerTeamMemberMapper">
    <resultMap id="BaseResultMap" type="com.community.entity.VolunteerTeamMember">
        <id property="id" column="id"/>
        <result property="teamId" column="team_id"/>
        <result property="userId" column="user_id"/>
        <result property="userName" column="user_name"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectByTeamId" resultMap="BaseResultMap">
        SELECT * FROM t_volunteer_team_member WHERE team_id = #{teamId}
    </select>

    <select id="selectByTeamAndUser" resultMap="BaseResultMap">
        SELECT * FROM t_volunteer_team_member WHERE team_id = #{teamId} AND user_id = #{userId}
    </select>

    <update id="incrementMemberCount">
        UPDATE t_volunteer_team SET member_count = member_count + 1 WHERE id = #{teamId}
    </update>
</mapper>
